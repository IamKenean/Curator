# Curator (Expo app)

This folder contains the Expo React Native app. Setup, run, and tunnel instructions are in the [root README](../README.md).

Quick start from this directory:

```bash
npm install
cp .env.example .env
npm start
```

Tunnel mode (phone on a different network):

```bash
npm run start:tunnel
```
